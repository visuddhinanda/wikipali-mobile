# CopilotKit Runtime 地址（Expo 会在 `npx expo start` 时内联 EXPO_PUBLIC_* 变量）
#
# ⚠️ 真机测试必须用「电脑的局域网 IP」，不能用 localhost/127.0.0.1
# 查看电脑 IP：Windows `ipconfig`；macOS/Linux `ifconfig` 或 `ip addr`
# 示例：http://192.168.1.100:3001/api/copilotkit
EXPO_PUBLIC_RUNTIME_URL=http://192.168.x.x:3001/api/copilotkit
